.nh
.TH "PC" "1" "Apr 2026" "Pinecone CLI" "Pinecone CLI Manual"

.SH NAME
.PP
pc-index-record-upsert - Upsert text records into an integrated index from a JSON/JSONL payload


.SH SYNOPSIS
.PP
\fBpc index record upsert [flags]\fP


.SH DESCRIPTION
.PP
Upsert records into an integrated index namespace from a JSON or JSONL payload.

.PP
The request --body/--file may be a JSON object containing "records": ...
\[la]a list of IntegratedRecord objects\[ra], a raw JSON array of records, or a
JSONL stream of IntegratedRecord objects. Bodies can be inline JSON,
loaded from ./file.json[l], or read from stdin with '-'.

.PP
Body schema: UpsertRecordsBody (records shaped like pinecone.IntegratedRecord:
https://pkg.go.dev/github.com/pinecone-io/go-pinecone/v5/pinecone#IntegratedRecord)


.SH OPTIONS
.PP
\fB-b\fP, \fB--batch-size\fP=96
	records per batch (max 96)

.PP
\fB--body\fP=""
	alias for --file

.PP
\fB--file\fP=""
	request body JSON or JSONL (inline, ./path.json[l], or '-' for stdin; only one argument may use stdin)

.PP
\fB-h\fP, \fB--help\fP[=false]
	help for upsert

.PP
\fB-n\fP, \fB--index-name\fP=""
	name of index to upsert into

.PP
\fB-j\fP, \fB--json\fP[=false]
	output as JSON

.PP
\fB--namespace\fP="\fBdefault\fP"
	namespace to upsert into


.SH OPTIONS INHERITED FROM PARENT COMMANDS
.PP
\fB--timeout\fP=1m0s
	timeout for commands, defaults to 60s (0 to disable)


.SH EXAMPLE
.EX
  $ pc index record upsert --index-name my-index --namespace my-namespace --body ./records.json
  $ pc index record upsert --index-name my-index --namespace my-namespace --body ./records.jsonl
  $ cat records.jsonl | pc index record upsert --index-name my-index --namespace my-namespace --body -

.EE


.SH SEE ALSO
.PP
\fBpc-index-record(1)\fP


.SH HISTORY
.PP
6-Apr-2026 Auto generated by spf13/cobra
