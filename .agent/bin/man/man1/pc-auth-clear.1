.nh
.TH "PC" "1" "Apr 2026" "Pinecone CLI" "Pinecone CLI Manual"

.SH NAME
.PP
pc-auth-clear - Clear a service account (client ID and secret) or API key from local storage


.SH SYNOPSIS
.PP
\fBpc auth clear [flags]\fP


.SH DESCRIPTION
.PP
Clear a service account (client ID and secret) or API key from local storage


.SH OPTIONS
.PP
\fB--api-key\fP[=false]
	Clear the default API key from local storage

.PP
\fB-h\fP, \fB--help\fP[=false]
	help for clear

.PP
\fB--service-account\fP[=false]
	Clear the configured service account (client ID and secret) from local storage


.SH OPTIONS INHERITED FROM PARENT COMMANDS
.PP
\fB--timeout\fP=1m0s
	timeout for commands, defaults to 60s (0 to disable)


.SH EXAMPLE
.EX
  # Clear configured service account credentials
  $ pc auth clear --service-account

  # Clear configured default API key
  $ pc auth clear --api-key

  # Clear both configured service account credentials and default API key
  $ pc auth clear --service-account --api-key

.EE


.SH SEE ALSO
.PP
\fBpc-auth(1)\fP


.SH HISTORY
.PP
6-Apr-2026 Auto generated by spf13/cobra
